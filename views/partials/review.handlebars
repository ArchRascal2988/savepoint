<div class="review">
    <a class='textBlk' href='api/games/single/{{game_id}}'>{{game.title}}</a>
    <h6 class="textBlu"> {{user.username}}'s review</h3>
        <section>
            <h4 class="textRed">{{rating}}</h4>
            <p class="textWhite" >{{content}}</p>
        </section>
</div>